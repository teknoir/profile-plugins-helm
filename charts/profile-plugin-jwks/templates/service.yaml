---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: jwks
    teknoir.org/plugin: jwks
  name: jwks
  namespace: {{ .Release.Namespace }}
spec:
  ports:
    - name: http
      port: 8083
      targetPort: 8083
  selector:
    app: jwks
